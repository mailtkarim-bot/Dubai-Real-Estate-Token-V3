# ============================================
# SEPOLIA DEPLOYMENT CONFIGURATION
# ============================================
# Copy this file to .env and fill in your real values.
# Never commit the real .env file.

# Deployer private key (hex, with 0x prefix)
export PRIVATE_KEY=0x...

# Sepolia RPC endpoint (Alchemy, Infura, or public RPC)
export SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Gnosis Safe address that will own the TimelockController.
# For a Sepolia demo, you can use your deployer address.
export GNOSIS_SAFE=0x...

# Address that will receive ISSUER_ROLE
export ISSUER_ADDRESS=0x...

# Address that will receive REGULATOR_ROLE
export REGULATOR_ADDRESS=0x...

# Timelock delay in seconds (default 48 hours)
export TIMELOCK_MIN_DELAY=172800

# Etherscan API key for contract verification
export ETHERSCAN_API_KEY=...
